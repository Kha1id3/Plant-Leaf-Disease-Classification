# Plant-Leaf-Disease-Classification
Plant leaf disease classification using transfer learning in PyTorch. Trains a ResNet-based CNN on public leaf datasets (e.g. PlantVillage), evaluates with scikit-learn (accuracy, F1, confusion matrix), and compares against simpler baseline models.
